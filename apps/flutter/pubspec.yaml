name: onyx
description: "Onyx - local-first task management"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  onyx_core:
    path: ../../packages/onyx_core
  provider: ^6.1.0
  window_manager: ^0.4.0
  file_picker: ^8.0.0
  google_fonts: ^6.0.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
